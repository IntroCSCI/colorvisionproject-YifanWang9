#ifndef COLORBLINDTEST_H
#define COLORBLINDTEST_H
#include <string>
#include <vector>

using std::string;
using std::vector;

class ColorBlindTest{
  public:
  void showColors(vector<string>);
  void colorTest(int &a);
};

#endif